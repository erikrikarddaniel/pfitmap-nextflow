#!/usr/bin/env nextflow

process singleFaa {

  input: 
  file(fastas) from Channel.fromPath("/home/ghada/dev/pfitmap-nextflow/testdata/genomes/*.faa.gz").collect()       

  output:
  file("all_genomes.faa") into hmmSearch_ch

  shell:
  """
  zcat $fastas > all_genomes.faa
  """
}
